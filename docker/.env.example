# WebTop Docker Environment Variables
# Copy this file to .env and adjust values

# Server Port (default: 3000)
PORT=3000

# Environment (production/development)
NODE_ENV=production

# Authentication
# IMPORTANT: Change JWT_SECRET in production!
JWT_SECRET=your-secure-secret-here-min-32-chars

# Default admin credentials
# Change these before first deployment
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-me-in-production

# Optional: Anthropic API Key for AI features
# ANTHROPIC_API_KEY=sk-ant-...

# ===========================================
# Docker Update Configuration
# ===========================================

# Docker Hub repository (default: peanuz/webtop)
DOCKER_REPOSITORY=peanuz/webtop

# Docker image tag to track (default: alpha)
# Change to "stable" or "latest" for stable releases
DOCKER_TAG=alpha

# Current version (set automatically during build)
# Format: MAJOR.MINOR.PATCH (e.g., 1.0.0, 1.0.1 for hotfixes)
# WEBTOP_VERSION=0.1.0

# Auto-update check interval in hours (default: 24)
UPDATE_CHECK_INTERVAL=24
